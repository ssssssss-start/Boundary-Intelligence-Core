# 反诈项目统一交付口径

本文件是交付说明、答辩材料和评测报告引用的统一口径。历史报告中的“生成时间”以及清单文件中的“打包时间”是证据时间戳，不等同于最终交付日期，不应改写。

| 项目 | 统一口径 |
|---|---|
| 交付日期 | **2026-08-02（Asia/Shanghai）** |
| 评测题量 | **702 条评测记录**：42 条离线基准 + 210 条开发集回归 + 400 条题库衍生回归 + 50 条真实风格评测 |
| Python | **3.11+**（以 `pyproject.toml` 的 `requires-python = ">=3.11"` 为准；推荐 3.11 或 3.12） |
| Node.js | **18+**（仅构建 `mobile-app` / H5 时需要；推荐 20 LTS） |
| 后端端口 | **8000：导入服务；8001：查询、聊天、管理后台与健康检查** |
| H5 开发端口 | **5173**（仅 `mobile-app` 本地开发，不属于后端交付端口） |
| 外部基础服务 | MongoDB 27017、Milvus 19530、FunASR 10096 |

## 模型说明

| 模型/能力 | 运行方式 | 是否随交付包提供 | 用途 |
|---|---|---:|---|
| `deepseek-chat` | DeepSeek OpenAI 兼容 API | 否，需配置 `OPENAI_API_KEY` | 文本对话、语义路由、RAG 总结 |
| `qwen-vl-plus` | DashScope OpenAI 兼容 API | 否，需配置 `VISION_API_KEY` | 截图 OCR 与视觉风险分析 |
| `BAAI/bge-m3` | 本地模型 | 否，需另行下载 | 知识库 dense + sparse 向量；不可用时可降级为 hash |
| Kokoro INT8 | 本地模型 | 是 | 本地语音合成 |
| FunASR | Docker 服务/本地模型目录 | 模型目录按环境配置 | 语音识别 |

`VL_MODEL=deepseek-chat` 只是旧兼容字段，视觉链路的有效配置是 `VISION_MODEL=qwen-vl-plus`。`BAAI/bge-reranker-large` 是预留配置，当前运行链路不要求下载。

## 评测口径边界

702 是四层评测的**执行记录总数**，不是 702 条独立盲测题。210 条与 400 条均属于内部回归；42 条覆盖三个不同子任务；50 条是真实风格、50 个独立案件族，但仍不是第三方盲测。因此不得把四组成绩直接平均或宣称为“702 条盲测准确率”。详见 `reports/judges/direction3_score_evidence_master.md`。

50 条数据报告中的“既有 710 条”只用于近似度检查，构成为 210 + 400 + 100 条试标模板；其中 100 条试标模板不属于本次四组正式成绩，所以不改变 702 条统一评测口径。
